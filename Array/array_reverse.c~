#include<stdio.h>
void main()
{	
	int i,j,k,l,n;
	int a[50];
	int temp=0;
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		scanf("%d",&a[i]);
	}
	for(i=0,j=n-1;i<n/2,j>=n/2;i++,j--)
	{
			temp=a[j];
			a[j]=a[i];
			a[i]=temp;
		}
	
	for(k=0;k<n;k++){
	printf("%d\t",a[k]);}
	
	
}
