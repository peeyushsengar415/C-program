#include<stdio.h>
void main()
{
	int i,j,k,l;
	int m,n,count=0;
	printf("enter 2 values");
	scanf("%d%d",&m,&n);
	for(i=m;i<=n;i++)
	{
		count=0;
		for(j=2;j<i;j++)
		{
			if(i%j==0)
			{
				count=1;
				break;
			}
		}
		if(count==0)
		printf("%d,",i);
	}
}
