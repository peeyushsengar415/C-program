#include<stdio.h>
void main()
{
	int i,j,k;
	int m,n;
	printf("enter 2 number");
	scanf("%d%d",&m,&n);
	for(i=m;i<=n;i++)
	{	
		temp=i;
		while(temp!=0)
		{
			r=n%10;
			sum=sum+r*r*r;
			
		}
	}
	
}
