#include<stdio.h>
void main()
{
	int n,i,j,count=0;
	printf("enter a number \n");
	scanf("%d",&n);
	for(i=2;i<n;i++)
	{
		if(n%i==0)
		{
			count=1;
			break;
		}
	}
	if(count==0)
	printf("%d is prime",n);
	else
	printf("number is not prime");
	
}
