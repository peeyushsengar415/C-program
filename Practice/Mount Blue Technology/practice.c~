#include<stdio.h>
int sum(int);
int main()
{
	int i,j,k;
	int n;
	int factorial=0;
	scanf("%d",&n);
	factorial=sum(n);
	printf("%d",factorial);
	
	
}
int 
int sum(int x)
{
	
	if(x<=1)
	return 1;
	else
	return x*sum(x-1);
}
