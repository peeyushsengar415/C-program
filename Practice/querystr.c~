#include<stdio.h>
int main()

{
    //int n,q;
    //n=4;
    //q=3;
    int n,i,q,count;
    char a[1000][20];
    char b[20];
    scanf("%d",&n);
    
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%s",a[i]);
    }
    scanf("%d",&q);
    while(q--)
    {
        count=0;
        scanf("%s",b);
        for(i=0;i<n;i++)
        {
            if(strcmp(b,a[i])==0)
            count++;
        }
        printf("%d\n",count);
    }
    return 0;
}
